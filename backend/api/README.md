# API App

FastAPI application skeleton for Step 0.

Current scope:

- app bootstrap
- health endpoint
- settings bootstrap
- API router structure

Run target:

- `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`
